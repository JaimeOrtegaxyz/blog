---
title: Hello, world
date: 2026-05-04
summary: First post — placeholder until the real one shows up.
---

This is a placeholder. Replace it with your first thought.

The file is plain markdown with a small front matter block at the top
(between the `---` lines). The fields are `title`, `date`, and an
optional `summary`.

The slug comes from the filename, so this post lives at
`/thoughts/welcome.html`.
